import Foundation

/*

offset
size
totalSize
chunkSize

total_count = total / chunk

page(chunk_number) = offset / chunk

pageOffset = offset - page * chunk


chunk_tail = chunk = pageOffset

number of chunks_to_read = size / chunk + 1

*/




